/**
 * ═══════════════════════════════════════════════════════════════
 * 카메라 줌 프리셋 (Camera Zoom Presets)
 * ═══════════════════════════════════════════════════════════════
 * 
 * 모든 UI 컴포넌트에서 카메라 줌 배율을 지정할 때 이 상수를 사용합니다.
 * 매직 넘버를 하드코딩하지 않고, 이 파일에서 중앙 관리합니다.
 * 
 * 엔진 레이어(PixiApplication, initMobileGestures, storeSync)는
 * 카메라 객체를 직접 제어하므로 이 상수의 적용 대상이 아닙니다.
 */

export const CAMERA_ZOOM = {
  /** 은하 전체 조감도 (로고 클릭 시) */
  GALAXY_OVERVIEW: 0.5,

  /** 일반 접속 초기 포커싱 (로그인 후 내 픽셀 위치) */
  INITIAL_ENTRY: 1.2,

  /** 특정 픽셀 포커싱 (클릭, 검색, URL 접속 등) */
  PIXEL_FOCUS: 0.5,

  /** 별자리 전체 조감 (별자리 탐색 이동 시) */
  CONSTELLATION_FOCUS: 0.35,
} as const
